# 贡献

为开源项目贡献代码，本身是一种高尚的行为，感谢所有人的付出。

## 如何贡献

首先你可以 `fork` [`icestack`](https://github.com/sonofmagic/icestack) 这个项目

### 贡献组件

> 建议先阅读 [`核心概念/添加组件`章节](/docs/core/extraComponent)

比如你要贡献一个组件，你可到 `packages/ui/src/components` 编写

并构建后，在根目录 `storybook` 项目添加 `story` 查看具体的效果

根目录的 `website` 里可以添加对应的文档
