// import { defineConfig } from '@icestack/ui'

export default {
  log: false,
  base: {
    themes: {
      light: {
        selector: 'page'
      },
      dark: {
        selector: '.dark'
      }
    }
  }
} // defineConfig()
