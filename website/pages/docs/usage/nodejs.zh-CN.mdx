# Nodejs API 调用

直接从 `@icestack/ui` 中导入方法进行调用

## API \(WIP\)

### transformCss2Js

`transformCss2Js(css: string): Record<string, any>`

快速把 `CSS` 转化成 `CssInJs` 结构。

### build

构建方法

### defineConfig

定义配置对象