# 从0开始构建组件库

使用 `IceStack` 构建组件库是非常简单的，只要你在各个网站上，看到好看的样式，你都可以把它们写成你的 Css 组件

比如你可以去 [awesome-tailwindcss](https://github.com/aniftyco/awesome-tailwindcss) 项目里找到许多的现成组件，

只需要少许的更改就能够变成我们自己的组件。而不加载 `@icestack/preset-default` 默认的那套组件，也只需要把 `mode` 设置为 `none` 就行

让我们立刻开始吧！
