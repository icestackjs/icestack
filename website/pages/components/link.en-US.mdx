import CssTable from '../../components/CssTable'
import Demo from '../../components/demo/link/index.mdx'

## Class Table

<CssTable name="link" />

## Demo

<Demo></Demo>

## Playground

[Go to Storybook](https://story.ui.icebreaker.top/?path=/docs/navigation-link--docs)

## Css Schema
<div className="collapse">
<input type="checkbox" /> 
<div class="collapse-title px-0">
<span class="btn">Click to Show Schema</span>
</div>
<div class="collapse-content"> 

```json
{
"selector": ".link",
"defaults": {
  "styled": {
    ".link": {
      "&-primary": {
        "apply": "text-primary [@media(hover:hover)]:hover:text-primary-active"
      },
      "&-success": {
        "apply": "text-success [@media(hover:hover)]:hover:text-success-active"
      },
      "&-warning": {
        "apply": "text-warning [@media(hover:hover)]:hover:text-warning-active"
      },
      "&-error": {
        "apply": "text-error [@media(hover:hover)]:hover:text-error-active"
      },
      "&-neutral": {
        "apply": "text-neutral [@media(hover:hover)]:hover:text-neutral-active"
      },
      "&:focus": {
        "apply": "outline-none"
      },
      "&:focus-visible": {
        "css": {
          "outline": "2px solid currentColor",
          "outline-offset": "2px"
        }
      }
    }
  },
  "base": {
    ".link": {
      "apply": "cursor-pointer underline",
      "&-hover": {
        "apply": "no-underline hover:underline"
      }
    }
  }
}
}
```

</div>
</div>