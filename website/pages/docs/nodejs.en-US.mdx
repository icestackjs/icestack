# Nodejs API calls

Calling methods directly from `@icestack/ui` import

## API \(WIP\)

### transformCss2Js

`transformCss2Js(css: string): Record<string, any>`

Quickly transforms `CSS` into a `CssInJs` structure.

### build

The build method

### defineConfig

Defines the configuration object