// import { defineConfig } from '@/index'

export default {
  log: false,
  base: {
    themes: {
      light: {
        selector: 'page'
      },
      dark: {
        selector: '.dark'
      }
    }
  }
}
