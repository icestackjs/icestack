/**
 * @type {import('@icestack/ui').Config}
 */
const config = {
  log: false,
  base: {
    themes: {
      light: {
        selector: 'page',
      },
      dark: {
        selector: '.dark',
      },
    },
  },
}

export default config
